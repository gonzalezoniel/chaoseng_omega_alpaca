services:
  - type: web
    name: chaoseng-omega-alpaca
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: PYTHONPATH=src uvicorn main:app --host 0.0.0.0 --port $PORT
    autoDeploy: true
